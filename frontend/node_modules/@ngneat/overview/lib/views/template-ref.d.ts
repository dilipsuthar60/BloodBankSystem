import { ApplicationRef, TemplateRef, ViewContainerRef } from '@angular/core';
import { ViewRef } from './types';
interface Args<C> {
    tpl: TemplateRef<C>;
    context: C;
    vcr: ViewContainerRef | undefined;
    appRef: ApplicationRef | undefined;
}
export declare class TplRef<C> implements ViewRef {
    private args;
    private viewRef;
    private element;
    private wrapper;
    constructor(args: Args<C>);
    detectChanges(): void;
    getElement(): Element;
    destroy(): void;
}
export {};
